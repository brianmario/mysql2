name: Build
on: [push, pull_request]

# A newer push to the same pull request obsoletes whatever is still in
# flight for it. Pushes to master are not cancelled -- every commit that
# lands there should get a run of its own.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # Temporarily disabled while developing build-windows.yml, so CI runs only
  # exercise Windows -- re-enable all of these before merging.
  # ubuntu:
  #   uses: ./.github/workflows/build-ubuntu.yml
  # macos:
  #   uses: ./.github/workflows/build-macos.yml
  # fedora:
  #   uses: ./.github/workflows/build-fedora.yml
  # cross-version:
  #   uses: ./.github/workflows/build-cross-version.yml
  windows:
    uses: ./.github/workflows/build-windows.yml
